# class-pet-management

当前开发与预览基线：

- `class-pet-vue`：正在维护的 Vue 3 前端。
- `class-pet-api`：正在维护的 Node.js + Express + MySQL 后端。
- `class-pet`：早期 React 原型，仅用于历史参考，不作为当前功能入口。

本地预览时启动 `class-pet-api` 与 `class-pet-vue`，默认使用 Google Chrome 浏览器打开前端地址。
